%h2 Editing project

- if i_own?(@project)
  = render 'form'
  = link_to "Delete this project", @project, :confirm => 'Are you sure?', :method => :delete
  %br/
  %br/
= link_to "Do not edit this project", @project
