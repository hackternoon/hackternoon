- if user_signed_in?
  %li
    Logged in as:
    %br/
    = link_to(truncate(current_user.email, :length => 15), sslhost + show_user_path(:user_id => current_user.id))
- else
  %li
    = link_to('Register', sslhost + new_user_registration_path)
